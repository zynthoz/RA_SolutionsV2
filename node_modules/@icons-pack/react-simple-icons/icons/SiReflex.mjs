import { jsxs, jsx } from 'react/jsx-runtime';
import * as React from 'react';

const defaultColor = "#6E56CF";
const SiReflex = React.forwardRef(function SiReflex2({ title = "Reflex", color = "currentColor", size = 24, ...others }, ref) {
  if (color === "default") {
    color = defaultColor;
  }
  return /* @__PURE__ */ jsxs(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      width: size,
      height: size,
      fill: color,
      viewBox: "0 0 24 24",
      ref,
      ...others,
      children: [
        /* @__PURE__ */ jsx("title", { children: title }),
        /* @__PURE__ */ jsx("path", { d: "M17 15v9h5v-9zM2 0v24h5v-9h10v-5H7V5h10v5h5V0z" })
      ]
    }
  );
});

export { SiReflex as default, defaultColor };
