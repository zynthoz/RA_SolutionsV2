
    import * as React from 'react';

    import { IconType } from '../types';

    type SiCodesandboxProps = React.ComponentPropsWithoutRef<'svg'> & {
      /**
       * The title provides an accessible short text description to the SVG
       */
      title?: string;
      /**
       * Hex color or color name or "default" to use the default hex for each icon
       */
      color?: string;
      /**
       * The size of the Icon.
       */
      size?: string | number;
    }

    const defaultColor = '#151515';

    const SiCodesandbox: IconType = React.forwardRef<SVGSVGElement, SiCodesandboxProps>(function SiCodesandbox({title = 'CodeSandbox', color = 'currentColor', size = 24, ...others }, ref) {
      if (color === 'default') {
        color = defaultColor;
      }

      return (
        <svg
          xmlns='http://www.w3.org/2000/svg'
          width={size}
          height={size}
          fill={color}
          viewBox='0 0 24 24'
          ref={ref}
          {...others}
        >
          <title>{title}</title>
          <path d='M0 24H24V0H0V2.45455H21.5455V21.5455H2.45455V0H0Z' />
        </svg>
      );
    });

    export { SiCodesandbox as default, defaultColor };
  