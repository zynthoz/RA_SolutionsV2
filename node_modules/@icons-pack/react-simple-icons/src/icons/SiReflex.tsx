
    import * as React from 'react';

    import { IconType } from '../types';

    type SiReflexProps = React.ComponentPropsWithoutRef<'svg'> & {
      /**
       * The title provides an accessible short text description to the SVG
       */
      title?: string;
      /**
       * Hex color or color name or "default" to use the default hex for each icon
       */
      color?: string;
      /**
       * The size of the Icon.
       */
      size?: string | number;
    }

    const defaultColor = '#6E56CF';

    const SiReflex: IconType = React.forwardRef<SVGSVGElement, SiReflexProps>(function SiReflex({title = 'Reflex', color = 'currentColor', size = 24, ...others }, ref) {
      if (color === 'default') {
        color = defaultColor;
      }

      return (
        <svg
          xmlns='http://www.w3.org/2000/svg'
          width={size}
          height={size}
          fill={color}
          viewBox='0 0 24 24'
          ref={ref}
          {...others}
        >
          <title>{title}</title>
          <path d='M17 15v9h5v-9zM2 0v24h5v-9h10v-5H7V5h10v5h5V0z' />
        </svg>
      );
    });

    export { SiReflex as default, defaultColor };
  